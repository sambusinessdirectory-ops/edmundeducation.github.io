import {hubRequest,escapeHtml as e} from './learning-hub-client.mjs';import {editMistake} from './study-support.mjs';
const system=new URLSearchParams(location.search).get('system')==='listening'?'listening':'reading',root=document.querySelector('#question-collection'),status=document.querySelector('#question-status');document.querySelector('h1').textContent=(system==='listening'?'Listening':'Reading')+' Mistake Logbook · 錯誤筆記簿';let offset=0,rows=[];
async function load(more=false){if(!more){offset=0;rows=[];}try{const next=await hubRequest('mistakes','list',{system,offset});offset+=next.length;rows.push(...next);root.innerHTML=rows.map((n,i)=>`<article class="question-entry"><h2>${e(n.context.section||n.context.title||system)} · ${e(n.context.year||n.context.practice||'')} · Q${e(n.context.question)}</h2><p>${e(n.body)}</p><details><summary>Question context · 題目內容</summary><p>${e(n.context.excerpt)}</p></details><button data-edit="${i}">Edit · 修改</button><a href="${e(safe(n.context.url))}">Open exercise · 返回練習</a></article>`).join('')||'<p>No notes yet · 尚未有錯誤筆記</p>';if(next.length===100)root.insertAdjacentHTML('beforeend','<button data-more>More · 更多</button>');status.textContent='';}catch(error){status.textContent=error.message;root.innerHTML='<a href="schedule-system.html">Student sign-in · 學生登入</a>';}}
function safe(url){try{const u=new URL(url,location.href);return u.origin===location.origin?u.href:'#';}catch{return '#';}}
root.onclick=e=>{const button=e.target.closest('[data-edit]');if(button)editMistake(rows[Number(button.dataset.edit)].context);if(e.target.closest('[data-more]'))load(true);};document.addEventListener('mistake-saved',()=>load());load();
