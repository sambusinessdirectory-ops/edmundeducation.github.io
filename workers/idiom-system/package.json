{
  "name": "edmund-idiom-system-worker",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "node --check src/index.js && npm test && wrangler deploy --dry-run",
    "catalog": "node ../../tools/generate-idiom-catalog.mjs",
    "test": "node --test test/*.test.mjs",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.36.0"
  }
}
