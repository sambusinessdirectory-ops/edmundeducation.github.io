{
  "name": "edmund-writing-submission-worker",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "corpus:check": "node ../../grammar-corpus/validate-and-generate.mjs",
    "check": "npm run corpus:check && node --check src/index.js && npm test && wrangler deploy --dry-run",
    "test": "node --test test/*.test.mjs",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.36.0"
  }
}
