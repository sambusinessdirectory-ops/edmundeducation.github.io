import fs from 'node:fs';import assert from 'node:assert/strict';import crypto from 'node:crypto';import {modules} from '../polysemy-lab/catalogue.mjs';
const root=new URL('../polysemy-lab/',import.meta.url),legacy=['american-female','american-male','british-male','british-female'],current=['american-female','british-male','british-female'];
const read=file=>JSON.parse(fs.readFileSync(new URL(file,root))),merged={...read('audio-new-local.json'),...read('audio-new-cloud.json'),...read('audio-16-32-local.json')},pending=new Set(read('audio-pending.json').questions),manifest={};
for(const m of modules.filter(m=>m.id!=='show'))for(const q of m.questions){const r=merged[q.id];if(!r){assert.ok(pending.has(q.id),'Missing unapproved audio: '+q.id);continue;}const voices=m.number>=16?current:legacy;assert.equal(r.voice,voices[q.sentenceIndex%voices.length]);assert.equal(r.text,q.en);assert.equal(r.sourceSha256,crypto.createHash('sha256').update(q.en).digest('hex'));assert.ok(fs.statSync(new URL(r.path,root)).size>1000);manifest[q.id]=r;}
for(const id of pending)assert.ok(!modules.filter(m=>m.number>=16).some(m=>m.questions.some(q=>q.id===id)),'New module unexpectedly pending: '+id);
fs.writeFileSync(new URL('audio-new.json',root),JSON.stringify(manifest,null,2)+'\n');console.log('Available audio manifest:',Object.keys(manifest).length,'pending:',pending.size);
