import {teamGrowth,relativeAge} from './momentum-core.mjs?v=20260916-ui-polish1';
import {escapeHtml as esc} from './library-core.mjs?v=20260916-ui-polish1';
const crown='<svg viewBox="0 0 24 24" aria-hidden="true"><path d="M3 7l5 4 4-7 4 7 5-4-2 12H5z"/><path class="badge-detail" d="M6 16h12M8 20h8"/><circle cx="12" cy="13" r="1.4" class="badge-jewel"/></svg>';
const trophy='<svg viewBox="0 0 24 24" aria-hidden="true"><path d="M7 3h10v6c0 4-2 6-5 6S7 13 7 9zM10 15h4v5h-4zM7 20h10v2H7z"/><path class="badge-detail" d="M7 5H3v3c0 3 2 4 5 4M17 5h4v3c0 3-2 4-5 4"/></svg>';
export function badgeHtml(member,tone){
 if(!tone)return '<span class="team-badge-space" aria-hidden="true"></span>';
 const flex=tone==='flex',place={gold:1,silver:2,bronze:3}[tone];
 return `<button type="button" class="team-rank-badge ${tone}" data-team-badge="${tone}" aria-label="${flex?'為 '+esc(member.username)+' 加油':'第 '+place+' 名 '+esc(member.username)+'，點擊慶祝'}" title="${flex?'一起加油！':'點擊皇冠慶祝！'}"><span class="badge-art">${flex?'<span class="badge-flex" aria-hidden="true">💪🏻</span>':`<span class="badge-crown">${crown}</span><span class="badge-trophy">${trophy}</span>`}</span></button>`;
}
export function totalHtml(course){const growth=teamGrowth(course);return `<div class="team-total-top"><strong>${Number(course.total_cards)||0}</strong><img class="team-total-flame" src="/assets/schedule/day-streak-fire.gif" width="76" height="90" alt=""/><span class="team-static-flame" aria-hidden="true">🔥</span></div><span class="team-total-label">全組累積完成題目 <svg class="pro-ui-arrow" viewBox="0 0 24 24" aria-hidden="true" focusable="false"><path d="M6 18 18 6M6 6h12v12"/></svg><small>Team questions completed</small></span>${growth?`<span class="team-day-growth"><span><b>+${growth.gain}</b> 題<small>過去 24 小時</small></span><span class="team-growth-percent">${growth.percent===null?(growth.gain?'新起點':'—'):'+'+growth.percent+'%'}<small>${growth.percent===null?'24 小時前為 0 題':'較 24 小時前'}</small></span></span>`:'<small class="team-growth-unavailable">24 小時變化載入中</small>'}`;}
export function updatedHtml(at,now=Date.now()){return at?`最新更新 <time datetime="${new Date(at).toISOString()}">${new Date(at).toLocaleString('zh-HK',{timeZone:'Asia/Hong_Kong',month:'numeric',day:'numeric',hour:'2-digit',minute:'2-digit',second:'2-digit',hour12:false})}</time> <span>（${relativeAge(at,now)}）</span><small>香港時間 · 自動更新</small>`:'正在取得最新資料…';}
export function celebrateBadge(button){if(button.classList.contains('celebrating'))return;button.classList.add('celebrating');const reduced=matchMedia('(prefers-reduced-motion: reduce)').matches;setTimeout(()=>button.classList.remove('celebrating'),reduced?800:1900);}
