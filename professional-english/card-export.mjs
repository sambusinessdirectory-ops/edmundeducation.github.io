import {escapeHtml as esc} from './library-core.mjs?v=20260916-ui-polish1';
export function cardPrintHtml({title,cards}){
 return `<!doctype html><html lang="zh-Hant"><head><meta charset="utf-8"><title>${esc(title)}</title><style>@page{size:A4;margin:14mm 12mm}*{box-sizing:border-box}body{font:13px/1.55 Arial,"Noto Sans TC","PingFang TC",sans-serif;color:#17382d}h1{font-size:25px;margin:0 0 8px}.brand{font-weight:bold;letter-spacing:.08em;color:#246448}.meta{color:#516d60}table{width:100%;border-collapse:collapse;table-layout:fixed}thead{display:table-header-group}th,td{padding:10px;border:1px solid #b8cfc1;text-align:left;vertical-align:top;overflow-wrap:anywhere;white-space:pre-line}th{background:#dcefe3;color:#17382d}tr{break-inside:avoid;page-break-inside:avoid}th:first-child,td:first-child{width:8%}.note{color:#4d6659;font-size:11px}.print-action{padding:12px;margin:12px 0}@media print{.print-action{display:none}body{-webkit-print-color-adjust:exact;print-color-adjust:exact}}</style></head><body><p class="brand">EdmundEducation · Professional English</p><h1>${esc(title)}</h1><p class="meta">${cards.length} 張字卡 · English / 中文</p><button class="print-action" onclick="window.print()">列印／另存為 PDF · Print / Save as PDF</button><table><thead><tr><th>#</th><th>English</th><th>中文翻譯</th></tr></thead><tbody>${cards.map((c,i)=>`<tr><td>${i+1}</td><td><strong>${esc(c.front)}</strong>${c.examples?.length?'<p>'+esc(c.examples.join('\n'))+'</p>':''}${c.attempts?`<p class="note">${Number(c.attempts)} 次嘗試</p>`:''}</td><td><strong>${esc(c.back)}</strong>${c.examples_zh?.length?'<p>'+esc(c.examples_zh.join('\n'))+'</p>':''}${c.note?'<p class="note">'+esc(c.note)+'</p>':''}</td></tr>`).join('')}</tbody></table><script>window.addEventListener('load',()=>document.fonts.ready.then(()=>setTimeout(()=>window.print(),200)));<\/script></body></html>`;
}
export function exportCardsPDF(options){if(!options.cards.length){alert('這個範圍沒有字卡。');return;}const win=window.open('','_blank');if(!win){alert('請允許開啟匯出視窗，再試一次。');return;}win.document.open();win.document.write(cardPrintHtml(options));win.document.close();}
