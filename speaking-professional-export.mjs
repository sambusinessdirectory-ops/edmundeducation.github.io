const xml=value=>String(value??'').replace(/[\x00-\x08\x0b\x0c\x0e-\x1f]/g,'').replaceAll('&','&amp;').replaceAll('<','&lt;').replaceAll('>','&gt;').replaceAll('"','&quot;');
const bytes=value=>new TextEncoder().encode(value);
function crc32(data){let crc=0xffffffff;for(const b of data){crc^=b;for(let i=0;i<8;i++)crc=(crc>>>1)^((crc&1)?0xedb88320:0);}return (crc^0xffffffff)>>>0;}
function record(size,values){const b=new Uint8Array(size),v=new DataView(b.buffer);for(const [offset,value,width] of values)width===2?v.setUint16(offset,value,true):v.setUint32(offset,value,true);return b;}
export function storedZip(files){const parts=[],central=[];let offset=0;for(const [path,content] of Object.entries(files)){const name=bytes(path),data=bytes(content),crc=crc32(data);const head=record(30,[[0,0x04034b50],[4,20,2],[8,0,2],[14,crc],[18,data.length],[22,data.length],[26,name.length,2]]);parts.push(head,name,data);central.push(record(46,[[0,0x02014b50],[4,20,2],[6,20,2],[16,crc],[20,data.length],[24,data.length],[28,name.length,2],[42,offset]]),name);offset+=head.length+name.length+data.length;}const centralSize=central.reduce((n,p)=>n+p.length,0);return new Blob([...parts,...central,record(22,[[0,0x06054b50],[8,Object.keys(files).length,2],[10,Object.keys(files).length,2],[12,centralSize],[16,offset]])],{type:'application/vnd.openxmlformats-officedocument.spreadsheetml.sheet'});}
export function workbookBlob(sheets){const files={};const names=Object.keys(sheets);files['[Content_Types].xml']=`<?xml version="1.0" encoding="UTF-8"?><Types xmlns="http://schemas.openxmlformats.org/package/2006/content-types"><Default Extension="rels" ContentType="application/vnd.openxmlformats-package.relationships+xml"/><Default Extension="xml" ContentType="application/xml"/><Override PartName="/xl/workbook.xml" ContentType="application/vnd.openxmlformats-officedocument.spreadsheetml.sheet.main+xml"/>${names.map((_,i)=>`<Override PartName="/xl/worksheets/sheet${i+1}.xml" ContentType="application/vnd.openxmlformats-officedocument.spreadsheetml.worksheet+xml"/>`).join('')}</Types>`;
files['_rels/.rels']='<Relationships xmlns="http://schemas.openxmlformats.org/package/2006/relationships"><Relationship Id="rId1" Type="http://schemas.openxmlformats.org/officeDocument/2006/relationships/officeDocument" Target="xl/workbook.xml"/></Relationships>';
files['xl/workbook.xml']=`<workbook xmlns="http://schemas.openxmlformats.org/spreadsheetml/2006/main" xmlns:r="http://schemas.openxmlformats.org/officeDocument/2006/relationships"><sheets>${names.map((name,i)=>`<sheet name="${xml(name)}" sheetId="${i+1}" r:id="rId${i+1}"/>`).join('')}</sheets></workbook>`;
files['xl/_rels/workbook.xml.rels']=`<Relationships xmlns="http://schemas.openxmlformats.org/package/2006/relationships">${names.map((_,i)=>`<Relationship Id="rId${i+1}" Type="http://schemas.openxmlformats.org/officeDocument/2006/relationships/worksheet" Target="worksheets/sheet${i+1}.xml"/>`).join('')}</Relationships>`;
for(const [i,name] of names.entries()){files[`xl/worksheets/sheet${i+1}.xml`]=`<worksheet xmlns="http://schemas.openxmlformats.org/spreadsheetml/2006/main"><sheetData>${sheets[name].map((row,r)=>`<row r="${r+1}">${row.map((value,c)=>{let col='',n=c+1;while(n){n--;col=String.fromCharCode(65+n%26)+col;n=Math.floor(n/26);}return typeof value==='number'&&Number.isFinite(value)?`<c r="${col}${r+1}"><v>${value}</v></c>`:`<c r="${col}${r+1}" t="inlineStr"><is><t xml:space="preserve">${xml(value)}</t></is></c>`;}).join('')}</row>`).join('')}</sheetData></worksheet>`;}return storedZip(files);}
